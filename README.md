```
Usage: ./push\_swap\_checker.sh [options] nb\_of\_tests lower\_bound upper\_bound nb\_of\_elm
Example:
 sh push\_swap\_checker.sh -c 150 -200 200 100
      > test checker
      > run 150 different tests with generated lists of 100 random numbers between -200 and 200
Options:
 -h, --help                Display usage
 -a, --all                 Check everything
 -c, --checker             Check checker
 -v, --verbose             Print tests
```
